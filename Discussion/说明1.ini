路径流量数据的字段结构：
每个生成的 *_paths.parquet 文件中的数据包含以下关键列：

track_id (int)：车辆的唯一标识符，用于追踪特定车辆在 15 分钟观测期内的完整轨迹。

edge_id (list/tuple)：核心路径数据。存储为一个经过有序压缩的路段 ID 序列。例如 [101, 105, 110] 表示车辆依次经过了这三个路段。

timestamp (datetime/float)：该趟行程的首个观测时间戳。它被定义为车辆进入路径第一个路段的时间，即按照出发时间定义流量。

path_len (int)：路径包含的去重路段总数。